{
  "name": "firebase-live-whiteboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "vitest run",
    "test:rules": "firebase emulators:exec --only auth,database 'vitest run --config vitest.rules.config.js'"
  },
  "dependencies": {
    "firebase": "^12.1.0",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase-tools": "^14.11.2",
    "vite": "^7.0.6",
    "vitest": "^3.2.4"
  }
}
